# Training split for cross-validation fold 4: every site except the four held out
# in fold 4 (see cv_folds in neophytes_names_colors.yaml).
defaults:
  - data
  - neophytes_names_colors
  - split_cv4
  - transforms_1024

data_name: 'np_cv4'
batch_size_train: 8
batch_size_val: 2
batch_size_test: 2
num_workers: 8
